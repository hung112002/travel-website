<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quản lý Địa điểm</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body class="admin-body">

    <header class="admin-header">
        <h1>adminダッシュボード</h1>
        <nav>
            <a href="/">ホームに戻る</a>
            <a href="/logout" class="btn-logout">ログアウト</a>
        </nav>
    </header>

    <main class="admin-container">
        <h2>場所のリスト</h2>
        
        <div class="admin-toolbar">
            <form action="/admin" method="GET" class="search-form">
                <input 
                  type="text" 
                  name="search" 
                  placeholder="Tìm kiếm tên, địa điểm..." 
                  value="<%= typeof keyword !== 'undefined' ? keyword : '' %>"
                >
                <button type="submit" class="btn">Tìm</button>
            </form>
            <a href="/admin/add" class="btn btn-primary">新しい場所を追加</a>
        </div>

        <table class="admin-table">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>地名</th>
                    <th>位置</th>
                    <th>状態</th>
                </tr>
            </thead>
            <tbody>
                <% if (travels.length > 0) { %>
                    <% travels.forEach(function(travel) { %>
                        <tr>
                            <td><%= travel.id %></td>
                            <td><%= travel.name %></td>
                            <td><%= travel.location %></td>
                            <td>
                                <a href="/admin/edit/<%= travel.id %>" class="btn btn-edit">修正</a>
                                <form action="/admin/delete/<%= travel.id %>" method="POST" style="display: inline-block;" onsubmit="return confirm('削除してもよろしいでしょうか');">
                                    <button type="submit" class="btn btn-danger">削除</button>
                                </form>
                            </td>
                        </tr>
                    <% }); %>
                <% } else { %>
                    <tr>
                        <td colspan="4">場所が見つかりません.</td>
                    </tr>
                <% } %>
            </tbody>
        </table>
    </main>
<script src="js/transition.js"></script>
</body>
</html>