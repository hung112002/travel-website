<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <title>パスワード再設定 | Shimane Travel</title>
    <link rel="stylesheet" href="/css/auth-style.css">
</head>
<body>
    <div class="auth-wrapper">
        <div class="luxury-card">
            <h2>FORGOT PASSWORD</h2>
            <% if (msg) { %>
                <div class="alert alert-success">✅ <%= msg %></div>
            <% } %>
            <p style="text-align: center; margin-bottom: 25px; font-size: 0.9rem; color: #666;">
                ご登録いただいているメールアドレスを入力してください。<br>
                パスワード再設定用のリンクをお送りします。
            </p>
            <form action="/forgot-password" method="POST">
                <div class="form-group">
                    <label>メールアドレス (Email)</label>
                    <input type="email" name="email" placeholder="example@gmail.com" required>
                </div>
                <button type="submit" class="btn-luxury">リンクを送信する</button>
            </form>
            <div class="auth-links">
                <a href="/login">ログインに戻る</a>
                <a href="/">ホームに戻る</a>
            </div>
        </div>
    </div>
    <script>
        window.addEventListener('load', function() {
            // Tìm đến phần tử Loader bằng ID
            const loader = document.getElementById('page-loader');
            
            if (loader) {
                // Tạo hiệu ứng mờ dần (Fade out)
                loader.style.opacity = '0';
                
                // Sau khi mờ hẳn (khoảng 0.5s), mới ẩn hoàn toàn khỏi layout
                setTimeout(() => {
                    loader.style.display = 'none';
                }, 500); 
            }
        });
    </script>
</body>
</html>