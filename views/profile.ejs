<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <title>マイプロフィール | Shimane Travel</title>
  <link rel="stylesheet" href="/css/auth-style.css">
</head>
<body>
  <% const favoritesSafe = (typeof favorites !== 'undefined' && favorites) ? favorites : []; %>

  <div class="auth-wrapper">
    <div class="luxury-card" style="max-width: 600px;">
      <div style="text-align: center; margin-bottom: 30px;">
        <span style="font-size: 80px;">👤</span>
        <h2 style="margin-top: 10px;">MY PROFILE</h2>
      </div>

      <div style="border-top: 1px solid #eee; padding-top: 25px;">
        <div style="display: flex; justify-content: space-between; margin-bottom: 20px;">
          <span style="font-weight: 700; color: #666;">ユーザー名</span>
          <span style="font-weight: 700; color: var(--primary);"><%= user.username %></span>
        </div>

        <div style="display: flex; justify-content: space-between; margin-bottom: 20px;">
          <span style="font-weight: 700; color: #666;">メールアドレス</span>
          <span><%= user.email %></span>
        </div>

        <div style="display: flex; justify-content: space-between; margin-bottom: 20px;">
          <span style="font-weight: 700; color: #666;">アカウント権限</span>
          <span style="color: var(--accent); font-weight: 700; text-transform: uppercase;">
            <%= user.role %>
          </span>
        </div>
      </div>

      <!-- FAVORITES (moved inside body + safe) -->
      <div style="margin-top: 50px; border-top: 1px solid #eee; padding-top: 30px;">
        <h3 style="font-family: 'Noto Serif JP'; color: var(--primary); margin-bottom: 20px;">
          🌟 お気に入りリスト (Danh sách yêu thích)
        </h3>

        <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 20px;">
          <% if (favoritesSafe.length > 0) { %>
            <% favoritesSafe.forEach(place => { %>
              <div style="border: 1px solid #ddd; padding: 15px; display: flex; gap: 15px; background: #fff;">
                <img src="<%= place.imageUrl %>" style="width: 100px; height: 70px; object-fit: cover;">
                <div>
                  <h4 style="margin: 0;"><%= place.name %></h4>
                  <a href="/travel/<%= place.id %>" style="color: var(--accent); font-size: 0.8rem; text-decoration: none;">
                    詳細を見る
                  </a>
                </div>
              </div>
            <% }) %>
          <% } else { %>
            <p style="color: #999;">お気に入りの場所はまだありません。</p>
          <% } %>
        </div>
      </div>

      <div style="margin-top: 40px;">
        <a href="/" class="btn-luxury" style="display: block; text-align: center; text-decoration: none;">ホームに戻る</a>
        <a href="/logout" style="display: block; text-align: center; color: #d32f2f; margin-top: 20px; font-weight: 700; text-decoration: none;">ログアウト</a>
      </div>
    </div>
  </div>
</body>
</html>
